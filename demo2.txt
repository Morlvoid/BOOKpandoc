<Window x:Class="BookPandoc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="假装出书 - BOOKpandoc" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F7"
        FontFamily="Microsoft YaHei">

    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="GrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#C5C5D6"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#4A4A6A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B5B5C6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 紫色按钮样式 -->
        <Style x:Key="PurpleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#7B68EE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7B68EE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6A5ACD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 输入框样式 -->
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A4A6A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Margin="{TemplateBinding Padding}"
                                         VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 标签样式 -->
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="Background" Value="#C5C5D6"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#4A4A6A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Label">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 下拉框样式 -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#7B68EE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7B68EE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border Background="#F5F5F7" BorderBrush="#C0C0D0" BorderThickness="2" CornerRadius="15" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Height="35" Margin="10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="假装出书 - BOOKpandoc" 
                          FontSize="16" 
                          FontWeight="SemiBold"
                          VerticalAlignment="Center"
                          Margin="10,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="−" Width="30" Height="25" 
                           Background="Transparent" BorderThickness="0" FontSize="16"/>
                    <Button Content="□" Width="30" Height="25" 
                           Background="Transparent" BorderThickness="0" FontSize="14"/>
                    <Button Content="×" Width="30" Height="25" 
                           Background="Transparent" BorderThickness="0" FontSize="16"/>
                </StackPanel>
            </Grid>

            <!-- 书名作者行 -->
            <Grid Grid.Row="1" Margin="15,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="书名：" Style="{StaticResource LabelStyle}" Width="70"/>
                <TextBox Grid.Column="1" Style="{StaticResource InputBoxStyle}" Margin="10,0"/>

                <Label Grid.Column="2" Content="作者：" Style="{StaticResource LabelStyle}" Width="70" Margin="15,0,0,0"/>
                <TextBox Grid.Column="3" Style="{StaticResource InputBoxStyle}" Margin="10,0"/>
            </Grid>

            <!-- 中间区域：下载按钮 + 文件列表 + 操作按钮 -->
            <Grid Grid.Row="2" Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧下载按钮 -->
                <Button Content="下载&#x0a;Pandoc" 
                       Style="{StaticResource GrayButtonStyle}"
                       Width="80" Height="80"
                       VerticalAlignment="Top"/>

                <!-- 中间文件列表 -->
                <Border Grid.Column="1" 
                       Background="White" 
                       BorderBrush="#C0C0D0" 
                       BorderThickness="2"
                       CornerRadius="10"
                       Margin="15,0">
                    <ListBox x:Name="FileListBox" 
                            BorderThickness="0" 
                            Background="Transparent"
                            Margin="5"/>
                </Border>

                <!-- 右侧操作按钮 -->
                <StackPanel Grid.Column="2" Orientation="Vertical" Width="100">
                    <Button Content="添加文件" Style="{StaticResource PurpleButtonStyle}" Height="40" Margin="0,0,0,8"/>
                    <Button Content="删除选中" Style="{StaticResource PurpleButtonStyle}" Height="40" Margin="0,0,0,8"/>
                    <Button Content="上移" Style="{StaticResource PurpleButtonStyle}" Height="40" Margin="0,0,0,8"/>
                    <Button Content="下移" Style="{StaticResource PurpleButtonStyle}" Height="40"/>
             <response clipped><NOTE>Result is longer than **10000 characters**, will be **truncated**.</NOTE>